============
Installation
============

At the command line::

    $ easy_install django-haystack-es

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-haystack-es
    $ pip install django-haystack-es
